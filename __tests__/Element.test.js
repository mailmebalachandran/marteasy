// import 'react-native';
// import React from 'react';
// import LoginScreen from '../src/screens/Login/LoginScreen';
// import renderer from 'react-test-renderer';

// let findElement = function(tree, element) {
  
//   let result = undefined;
//   for (node in tree.children) {
//     for(scrollNode in tree.children[node].children){
//         console.log(tree.children[node].children[scrollNode].children[2].children[0].props);
//             // for(textNode in tree.children[node].children[scrollNode].children){
//             //     for(textboxNode in tree.children[node].children[scrollNode].children[textNode]){
//             //         if(tree.children[node].children[scrollNode].children[textNode].children[textboxNode] !== undefined)
//             //         {
                    
//             //         }
//             //     }
//             // }
//     }
//   }
//   return result;
// };

// it('find element', () => {
//   let LoginData = renderer.create(<LoginScreen />).toJSON();
//   expect(findElement(LoginData, 'username')).toBeDefined();
// });
