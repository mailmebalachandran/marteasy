import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  flexRow: {
    flexDirection: 'row',
  },
  subHeading: {
    marginTop: '2%',
    fontSize: 13,
    fontWeight: '100',
  },
  addAddrBtnContainer: {
        borderRadius: 0,
    },
  ViewContainerStyle:{
    flex:1,
    flexDirection:'row',
    padding:10
  }
});

export default styles;
